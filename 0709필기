생명주기
1 내용가져오기 리액트 바벨등등
2 선언해주기 App 내용만 담았고 실행은 안된상태
3 ReactDOM 여기서부터 App를 실행 시킨다.
4 ReactDOM.render 어디로 보낼거냐 #root로 보낼거다
이때 App이 실행된다.
5 class는 함수를 묶어주는 역할일뿐 실행은 안된다.
6 ReactDOM.render 실행 >> App 실행 >> App안에 render 실행
7  componentDidMount() render 끝나면 이 함수를 실행시켜라



쓰레드
자쓰는 안 쉬어주고 실행한다.(싱글쓰레드)
1,3,2출력 셋타임 아웃으로 1초를 줘서 132출력된다.

파이썬은 쉬어주고 실행한다 먼말인지 모르겠다.(멀티쓰레드))
1,2,3출력 3에 셋타임 아웃 1초를 줘도 123출력된다.

스택에 들어와 나갈때 실행이 된다
셋타임아웃을 준 엘리먼트는 시간이 걸린다는걸 알고 다른 박스로 이동시켜 놓고
그다음 엘리먼트를 스택에 넣고 내보내면서 실행시킨다.
결과는 132

함수형에서 api를 어떻게 쓰는가

함수와 클래스 실행속도 차이는 거의없다 




app 로그인 정보
loginbox
login
button